<<<<<<< SEARCH
                        safe_filename = member.filename.lstrip("/\\")
                        extracted_path = os.path.realpath(
                            os.path.join(tmp_dir_real, safe_filename)
                        )

                        try:
                            if (
                                os.path.commonpath([tmp_dir_real, extracted_path])
                                != tmp_dir_real
                            ):
                                raise ValueError("Path traversal detected")
                        except ValueError:
                            raise HTTPException(
                                status_code=400,
                                detail=f"Unsafe zip file detected: {safe_name}",
                            )
=======
                        safe_filename = member.filename.lstrip("/\\")
                        extracted_path = os.path.realpath(
                            os.path.join(tmp_dir_real, safe_filename)
                        )

                        try:
                            # os.path.realpath(tmp_dir) correctly resolves symlinks.
                            # Ensure that safe_filename has not traversed out of the parent directory.
                            if (
                                os.path.commonpath([tmp_dir_real, extracted_path])
                                != tmp_dir_real
                            ):
                                raise ValueError("Path traversal detected")
                        except ValueError:
                            raise HTTPException(
                                status_code=400,
                                detail=f"Unsafe zip file detected: {safe_name}",
                            )
>>>>>>> REPLACE
